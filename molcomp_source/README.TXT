How to compile under Linux:

1) Download folder molcomp_source
2) Open terminal & go to the downloaded folder
3) Install Gnu Scientific Library (GSL)
  Ubuntu: sudo apt install libgsl-dev
4) Compile code via running:
  bash compile.bash
5) rename a.out to molcomp (if you like), or just use it as a.out
